_schema-version: 3.3.0
ID: employeeManagement_OJT
description: A simple CAP project.
version: 1.0.0
modules:
- name: employeeManagement_OJT-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: employeeManagement_OJT-auth
  - name: employeeManagement_OJT-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: employeeManagement_OJT-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: employeeManagement_OJT-db
  parameters:
    buildpack: nodejs_buildpack
- name: employeeManagement_OJT
  type: approuter.nodejs
  path: app/router
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: employeeManagement_OJT-auth
  - name: employeeManagement_OJT-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  provides:
  - name: app-api
    properties:
      app-protocol: ${protocol}
      app-uri: ${default-uri}
  parameters:
    disk-quota: 256M
    keep-existing-routes: true
    memory: 256M
- name: employeeManagement_OJT-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: employeeManagement_OJT-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - employeemanagement.zip
      name: employeemanagement
      target-path: resources/
- name: employeemanagement
  type: html5
  path: app/employee_management
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: employeeManagement_OJT-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        redirect-uris:
        - https://~{app-api/app-uri}/**
      tenant-mode: dedicated
      xsappname: employeeManagement_OJT-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
  requires:
  - name: app-api
- name: employeeManagement_OJT-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: employeeManagement_OJT-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: employeeManagement_OJT-html5-service
    service-plan: app-host
- name: employeeManagement_OJT-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: employeeManagement_OJT-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
